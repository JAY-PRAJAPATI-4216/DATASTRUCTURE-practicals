#include <iostream>
using namespace std;

struct Node {
    int data;
    struct Node *next;
    struct Node *prev;
};

struct Node *first = NULL ,*last = NULL;

void insert_first(){
    struct Node *temp;
}
int main(){

}